# Derived pipeline intermediates -- regenerated by analysis/ scripts on every run.
# Excluded from the repo so it stays small (synthetic_trade_data.csv is the only ~50MB file).
analysis/cleaned_stage_p1.tsv
analysis/cleaned_p1_final.tsv
analysis/06_row_flags.tsv
analysis/quarantine_stage_p1.tsv

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
.pytest_cache/

# Word lock files (created when a docx is open in Word)
~$*.docx
~$*.doc

# Editor / OS noise
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/

# Fallback docx outputs (only used when primary path is locked)
deliverable/*_v2.docx
