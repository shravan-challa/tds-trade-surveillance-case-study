# Quarantine bucket (always 0 rows on the canonical run; included only if non-empty)
analysis/quarantine_stage_p1.tsv

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
.pytest_cache/

# Word lock files (created when a docx is open in Word)
~$*.docx
~$*.doc

# Editor / OS noise
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/

# Fallback docx outputs (only used when primary path is locked)
deliverable/*_v2.docx
